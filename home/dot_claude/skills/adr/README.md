# adr skill

Derived from the `architecture` skill in [anthropics/knowledge-work-plugins](https://github.com/anthropics/knowledge-work-plugins/tree/main/engineering/skills/architecture), licensed under Apache 2.0 (see `LICENSE`).

## Local changes

- Renamed skill from `architecture` to `adr`;
- Rewrote `description` frontmatter to use trigger-phrase style 
- Removed the `## Usage` section and the `/architecture $ARGUMENTS` invocation block
- Removed the `CONNECTORS.md` reference and the `## If Connectors Available` section